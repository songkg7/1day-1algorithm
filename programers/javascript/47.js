let n = 5;
let m = 3;

function solution(n, m) {
  return Array(m)
    .fill()
    .map(() => "*".repeat(n))
    .join("\n");
}

console.log(solution(n, m));

for (let i = 0; i < b; i++) {
  result += "*".repeat(a) + "\n";
}
console.log(result);
